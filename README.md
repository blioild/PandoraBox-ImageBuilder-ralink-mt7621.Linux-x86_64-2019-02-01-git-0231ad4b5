```bash
sudo apt-get update && sudo apt-get install vim golang npm build-essential libncurses5-dev zlib1g-dev gawk git libssl-dev wget unzip python ocaml-nox help2man texinfo yui-compressor
make info
vim .profiles.mk
make image PROFILE=xiaomi-r3g FILES=files/
```
commad FILES is optional
